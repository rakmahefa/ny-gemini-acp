use super::*;

// State tests that exercise Store persistence and turn state live here.
// ACP turn waiting/cancellation is tested by gemini-acp-encaps instead of
// retaining the removed Store::wait_prompt_done/prompt_handle mechanism.
